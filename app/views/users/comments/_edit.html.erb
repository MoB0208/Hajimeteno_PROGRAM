<%= form_with model: [@post, @comment], url: post_comment_path(post), local:true do |f| %>
  <div class="form-group text-center">
    <%= f.text_area :comment_body, size: "100x3", placeholder: "コメントはこちら" %>
    <%= f.submit "再送する", class: "btn btn-success" %>
  </div>
<% end %>