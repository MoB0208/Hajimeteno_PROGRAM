<!--管理者用言語一覧検索用-->
<div>
  <h2>言語一覧</h2>
  <% genres.each do |genre| %>
    <table>
      <tr>
        <%= link_to genre.genre_name, admins_genre_path(genre) %>
      </tr>
    </table>
  <% end %>
</div>